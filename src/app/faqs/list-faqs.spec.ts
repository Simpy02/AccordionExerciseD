import { ListFaqsComponent } from './list-faqs.component';

 describe('ListFaqsComponent',() => {
   it('Should Initiate ',() => {
     const result = new ListFaqsComponent(-1);
     expect(result).toBe(1);

   })
 })
